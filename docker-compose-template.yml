version: '3'

services:
  ipchanger:
    container_name: changeip
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DOPPLER_TOKEN: $DOPPLER_TOKEN
      TZ: Europe/Bucharest
    restart: always